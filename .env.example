# Names only. Store real values outside Git and never paste them into chat.
# Browser-safe Stripe sandbox key. This is reserved for the future client flow.
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
# Server-only Stripe sandbox API key. Never expose it through NEXT_PUBLIC_*.
STRIPE_SECRET_KEY=
# Server-only, endpoint-specific signing secret for /api/webhooks/stripe.
STRIPE_WEBHOOK_SECRET=
